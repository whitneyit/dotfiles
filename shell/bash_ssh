#!/bin/bash

# Only do this SSH setup if we are not in a vagrant instance
if [ $(os) == "Vagrant" ]; then

    #
    ssh-agent

    #
    ssh-add ~/.ssh/james@whitney.io
    ssh-add ~/.ssh/james.whitney@tempest.io

fi

# vim: set syn=sh :
